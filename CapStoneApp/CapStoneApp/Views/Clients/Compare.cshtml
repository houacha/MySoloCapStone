@model CapStoneApp.Models.ApiViewModel
@{
    ViewBag.Title = "Compare";
}

<h2>Compare</h2>

<h4>Compare Candidates:</h4>
@using (Html.BeginForm())
{
    <div class="form-group">
        <div class="col-md-3">
            @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control", @value = " " } })
            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-3">
            @Html.EditorFor(model => model.Name2, new { htmlAttributes = new { @class = "form-control", @value = " " } })
            @Html.ValidationMessageFor(model => model.Name2, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-6">
            <input type="submit" value="Search" class="btn btn-default" />
        </div>
    </div>
}
}