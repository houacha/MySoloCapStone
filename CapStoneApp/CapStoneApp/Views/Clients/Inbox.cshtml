@model IEnumerable<CapStoneApp.Models.InboxMessege>

@{
    ViewBag.Title = "Inbox";
}

<h2>Inbox</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Messege)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Messege)
        </td>
        <td>
            @Html.ActionLink("Delete", "MessegeDelete", new { id=item.Id })
        </td>
    </tr>
}

</table>
